---
layout: default
title: Daangn Interview Notes
nav_exclude: true
search_exclude: true
sitemap: false
description: "Private Daangn backend interview notes"
permalink: /cv/daangn-interview/
---

이 문서는 당근 커뮤니티 백엔드 면접 대비용 개인 메모입니다.

답변은 길게 외우기보다, **문제 관측 -> 원인 분리 -> 선택지 비교 -> 구현 -> 측정 -> 추가 개선** 흐름으로 말하는 것을 기준으로 정리했습니다.

## Redis Stream MQ 알림 시스템

### 기존 알림 구조에서 가장 큰 병목은 정확히 어디였나요?

주문 완료 흐름 안에서 외부 Push API 호출이 직접 실행되는 점이 가장 큰 병목이었습니다. 외부 API timeout/retry가 주문 응답 시간과 알림 처리 안정성에 직접 영향을 주는 구조였습니다.

### Sentry에서 Push API timeout을 봤다고 했는데, 그게 사용자 영향으로 어떻게 이어진다고 판단했나요?

Sentry에서 특정 사용자 주문 push 전송 중 timeout을 확인했었어요.

### 그냥 비동기 처리나 thread pool 분리로는 왜 부족했나요?

비동기처리는 좋지만 서버가 중간에 OOM 나서 재시작하면 트레이스가 안되거든요. 그래서 좀 부족했습니다.

### Kafka가 아니라 Redis Stream을 선택한 이유는 무엇인가요?

크게 2개가 있는데 첫번째는 낮은 운영비용이고 두번쨰는 메세지 소비 순서가 상관없어서 선택하게 되었습니다. 

### Redis Stream을 메시지 큐로 쓸 때 Kafka 대비 포기한 것은 무엇인가요?

메모리 휘발성리스크를 가져갔고 메세지 순서를 포기했습니다.

### 메모리 휘발문제를 어떻게 대처했나요?

redis 에서 aof 옵션을 everysec 로 최대 1초 유실 설정했습니다. 그리고 outbox 테이블을 원장으로 두고 스케줄러 따로 돌리면서 누락감지하면 최근 1분간 메세지를 재적재하도록 하였습니다.

### Consumer Group과 PEL이 정확히 어떤 문제를 해결했나요?

Consumer Group은 여러 consumer가 메시지를 나눠 처리하게 해주고, PEL은 consumer가 가져갔지만 ACK하지 않은 메시지를 추적합니다. 그래서 PEL 에 claim 해서 미완료된 애들을 다시 재처리할 수 있죠.

### at-least-once 시맨틱이면 중복 전송 가능성이 있는데, 알림 중복은 어떻게 제어했나요?

produce 할 때 idempotent key 랑 같이 넣을 수 있어서 동일한 메세지 id 에 한해서 재produce 안되고 consuming 은 xreadgroup 쓰면 자동적으로 동일한 그룹내 소비를 막아줍니다. 또 소비할 수도 있거든요? 재처리때문에? 소비 이벤트 저장이랑 내부 비즈니스로직 트랜젝션이랑 하나로 엮으면 되요.

### ACK 전에 서버가 죽으면 어떻게 복구되나요?

ACK 전에 서버가 죽으면 메시지는 PEL에 남습니다. 일정 idle time이 지난 메시지를 재처리하는 6.2 부터 추가된 XAUDOCLAIM 이 있어요. 스케줄러를 따로 돌려서 그걸로 재처리하도록 설계했었습니다.

### 실제로 타임아웃이 나는 경우는 극소수였을 것 같은데 굳이 이렇게 설계했어야했나?

대용량 푸시전송 배치시간이랑 겹치면 실제로 타임아웃이 났었고 대부분은 retry 하면 해결되었지만 그럼에도 불구하고 누락되는 메세지가 있었습니다. 그래서 이런식으로 설계할 수 밖에 없었습니다. 그리고 꽤 흔하게 일어나더라구요 타임아웃이. 다른 사례들 특히 24년도 우아한테크 세미나 보면 거기는 15분동안 장애가 일어났었다고 하더라구요.

### ACK 후 외부 Push API 응답 처리 중 장애가 나면 어떻게 되나요?

재처리하기 좀 힘들어요. 그래서 그냥 ack 는 push api 완료 후 전송하도록 하면되죠.

### 결국 사용자 최종 push 는 알 수 없는거 아니냐?

사용자가 최종 push 를 봤냐까진 설계범위에 없었어요.

### XAUTOCLAIM/XCLAIM을 언제 쓰고, 어떤 기준으로 claim 대상을 정했나요?

consumer가 메시지를 가져간 뒤 일정 시간 이상 ACK하지 않은 경우 재처리 대상으로 봤습니다. idle time과 consumer heartbeat를 함께 보고, 죽은 consumer가 잡고 있던 메시지를 다른 consumer가 회수하도록 했습니다.

### consumer heartbeat를 왜 별도로 관리했나요?

redis stream 은 live 한 consumer 을 보여주진 않아요. 모든 conusmer 을 보여줘서 live 한 컨슈머관리가 메세지 재처리 위임때문에 필요했습니다.

### Redis Stream이 장애나 메모리 압박을 받으면 알림 시스템은 어떻게 되나요?

stream 으로 인한 메모리 압박을 받지 않도록 설계했습니다. stream 별로 최대길이 설정해주고 trimming 하도록 설정하면 됩니다. 그리고 push 발송의 경우엔 전송완료하면 del 시켜서 큐 압박없게 했습니다.

### DLQ/CDL로 보낸 메시지는 이후 어떻게 운영자가 확인하거나 재처리하나요?

정상 큐에서 분리된 실패 메시지를 별도 stream으로 보관하고, 실패 원인과 retry 횟수를 확인할 수 있게 했습니다. 운영자는 원인을 확인한 뒤 재적재하거나 수동 처리하는 방식으로 복구할 수 있습니다.

### outbox 테이블을 도입했다고 했는데, DB transaction과 Redis produce 사이 정합성은 어떻게 보장했나요?

produce 이전에 db 에 이벤트 즉시 커밋하고 이후 produce 완료 후 해당 이벤트를 바로 update 하도록 했습니다. 그리고 별로 스케줄러로 실패건들 idempotent key 옵션키고 재적재하도록 설계했습니다.

### 이 구조를 다시 만든다면 Redis Stream을 계속 선택할 건가요?

동일 상황이면 지금은 그게 최선이라고 생각합니다. 이벤트 처리 순서가 중요해지거나 장기 보관이 필요하면 Kafka 사용할 것 같습니다.

## CDC / ETL / MongoDB 동기화

### 왜 MySQL 정규화 모델만으로는 어드민 조회 요구사항을 만족하기 어려웠나요?

회원추출이라는 기능이 여러 join 이 과도하게 일어나는 작업이였거든요. 주문, 결제, 메뉴, 모든 도메인에 걸쳐서 일어나는 작업이였습니다. 그래서 비정규화시켜 join 없이 즉시 조회하도록 하는게 필요했어요.

### 배치로 동기화하진 않고 실시간이 왜 필요했나요?

배치도 가능은 했지만 주문을 모두 select batch 로 확인해서 변경분 파악후 반영하는데 시간이 오래걸릴뿐더러 부하가 있을거라고 판단했습니다. 그래서 CDC 로 초기 스냡샷 제외 변경분 파악만 하도록 설정했습니다.

그리고 어드민이 이벤트 발행하고 오늘 특정메뉴를 구입한 사람들을 체크하고싶어해요. 그러면 못해도 30분마다 배치를 돌려야하는데, 이러면 무거운 작업이 30분마다 진행되는거에요. 비효율적이죠.

### schema 변경이 일어나면 어떻게 대처하는지

read model 반영 필요하다면 sink 모듈 배포 후 debezium 에 시그널 줘서 증분스냅샷으로 동기화시키면 됩니다.

### 스냅 샷 도중에 schema 변경일어나면?

만약 발생했다면 snapshot 을 중단하고 connector/schema history 상태를 확인한 뒤 다시 snapshot 을 수행하는 게 안전합니다.


-------


### schema 변경 때문에 CDC 파이프라인이 멈추면 어떻게 복구하나요?

우선 실패한 이벤트의 offset 과 key 를 확인해야 합니다. 이벤트 자체가 변환 불가능한 경우 DLQ 로 빼고 다음 이벤트 처리를 이어가게 해야 합니다. 이후 mapper 수정 후 DLQ 이벤트를 재처리하거나, 특정 key 범위를 MySQL 원본 기준으로 다시 읽어서 MongoDB read model 을 재생성합니다.

### schema 변경 대응에서 가장 중요한 기준은 무엇인가요?

CDC 파이프라인이 schema 변경 하나 때문에 전체 중단되면 안됩니다. 모르는 필드는 무시하고, 필수 필드 누락이나 타입 불일치는 실패로 분리하고, 원본 MySQL 기준으로 재처리 가능해야 합니다. 결국 핵심은 sink 가 변경에 tolerant 해야 하고, 조회 모델은 언제든 재생성 가능해야 한다는 점입니다.


### MongoDB 역정규화 모델을 선택한 이유는 무엇인가요?

조회 목적에 맞게 주문/메뉴 데이터를 한 문서 구조로 묶어 빠르게 읽기 위해서였습니다. 쓰기 원본은 MySQL에 두고, 어드민 조회는 MongoDB 역정규화 모델로 분리하는 구조가 요구사항에 맞았습니다.

### Elasticsearch가 아니라 MongoDB를 선택한 이유는 무엇인가요?

주요 요구사항은 전문 검색보다 어드민 조회용 문서 모델과 빠른 key 기반 조회였습니다. 검색 랭킹이나 복잡한 analyzer보다 데이터 동기화와 문서 upsert가 중요했기 때문에 MongoDB가 더 단순했습니다.

### MySQL CDC를 직접 구현하지 않고 Debezium을 쓴 이유는 무엇인가요?

binlog 처리, offset 관리, schema change 대응은 직접 구현하면 위험도가 높습니다. Debezium은 검증된 CDC 도구이기 때문에 변경 이벤트를 안정적으로 읽는 역할에 집중시키는 것이 낫다고 판단했습니다.

### CDC 이벤트 순서가 꼬이면 MongoDB 데이터 정합성은 어떻게 보장하나요?

이벤트에는 기준이 되는 key와 변경 시점이 있어야 하고, sink에서는 upsert가 idempotent하게 동작해야 합니다. 순서 문제가 생길 수 있는 구간은 partition key와 version/timestamp 기준으로 방어해야 합니다.

### 중복 이벤트가 들어오면 upsert는 idempotent하게 동작하나요?

같은 비즈니스 key 기준으로 upsert하도록 설계하면 중복 이벤트가 들어와도 최종 상태는 동일하게 수렴합니다. CDC/ETL에서는 중복 제거보다 idempotent한 write 모델을 만드는 것이 더 중요합니다.

### Redis Stream MQ가 CDC 파이프라인 중간에 들어간 이유는 무엇인가요?

Debezium과 ETL sink를 직접 연결하면 sink 장애나 처리 지연이 CDC 흐름에 바로 영향을 줍니다. Redis Stream MQ를 중간에 두면 produce와 consume을 분리하고, 장애 후 재처리 기준을 만들 수 있습니다.

### ETL sink pod가 죽으면 어디서부터 다시 처리하나요?

Redis Stream Consumer Group 기준으로 ACK되지 않은 메시지는 PEL에 남습니다. sink pod가 죽으면 idle time이 지난 pending 메시지를 다른 consumer가 회수해 다시 처리할 수 있습니다.

### Redis Stream memory가 4GB까지 증가했다고 했는데, 원인은 무엇이었나요?

produce 속도보다 consume 속도가 느리거나, stream trimming 기준이 부족하면 메시지가 쌓이면서 메모리가 증가합니다. CDC 파이프라인에서는 순간 유입량과 sink 처리량 차이를 계속 관측해야 했습니다.

### 512MB limit / 85% threshold consume 제한은 어떤 기준으로 정했나요?

Redis가 메모리 한계에 가까워지기 전에 consume/produce 흐름을 제어하기 위한 운영 기준이었습니다. 85%를 넘기기 전에 압박을 감지하고 처리량을 조절해 장애로 번지지 않도록 했습니다.

### backpressure를 걸면 데이터 동기화 지연이 생기는데, 사용자 경험과 어떻게 균형을 잡았나요?

동기화 지연은 허용 가능한 범위 안에서 관리하고, Redis 메모리 장애는 반드시 피해야 했습니다. 어드민 조회는 약간의 지연보다 시스템 안정성이 더 중요했기 때문에 backpressure를 선택했습니다.

### 증분 스냅샷은 어떤 상황에서 필요했고, 전체 스냅샷 대비 어떤 장점이 있었나요?

전체 데이터를 매번 크게 다시 처리하면 부하와 시간이 커집니다. 증분 스냅샷은 변경 구간을 나누어 반영하므로 재처리 범위를 줄이고, 운영 중 동기화 부담을 낮출 수 있습니다.

### MongoDB bulk upsert에서 100건 평균 27ms라고 했는데, 측정 환경과 기준은 무엇인가요?

단건 upsert를 반복했을 때와 bulk upsert로 묶어 처리했을 때의 sink 처리 시간을 비교했습니다. 단건 1.3~1.5ms 수준이던 처리를 100건 평균 27ms로 묶어 네트워크 왕복과 write overhead를 줄였습니다.

### 이 파이프라인에서 가장 위험한 장애 지점은 어디라고 보나요?

Redis Stream에 메시지가 계속 쌓이는 구간이 가장 위험합니다. 원본 DB 변경은 계속 들어오는데 sink가 따라가지 못하면 메모리 압박, 동기화 지연, 재처리 증가가 함께 발생할 수 있습니다.

### 운영 중 데이터 불일치를 감지하는 방법은 있었나요?

원본 MySQL과 MongoDB 조회 모델의 주요 key/count/checksum을 비교하는 검증 흐름이 필요합니다. 운영에서는 특정 기간이나 특정 주문 기준으로 샘플링 검증을 두는 것이 현실적입니다.

## 배치 성능 개선

### 19m13s에서 53s로 줄였다고 했는데, 가장 큰 개선 요인은 무엇이었나요?

가장 큰 요인은 offset pagination 제거와 bulk update 적용이었습니다. 여기에 connection pool, chunk size, VirtualThread, select column 축소를 함께 적용해 전체 처리 시간을 줄였습니다.

### offset pagination이 왜 느려지는지 설명해 주세요.

offset이 커질수록 DB는 앞의 많은 row를 건너뛰어야 합니다. 뒤쪽 페이지로 갈수록 버리는 데이터가 많아져 대용량 배치에서는 점점 느려집니다.

### sliding-window id range 조회는 어떤 조건에서 안전하게 동작하나요?

정렬 기준이 안정적이고, 범위 조건으로 다음 구간을 명확히 가져올 수 있을 때 안전합니다. id가 연속적일 필요는 없지만, 마지막 처리 id를 기준으로 다음 범위를 조회할 수 있어야 합니다.

### id가 연속적이지 않거나 중간에 삭제된 데이터가 있으면 어떻게 처리되나요?

id가 비어 있어도 `id > last_id` 방식이면 다음 존재하는 row를 가져올 수 있습니다. 삭제된 id는 자연스럽게 건너뛰고, 마지막 처리 위치만 정확히 관리하면 됩니다.

### chunk size를 100에서 5로 줄였는데, 일반적으로는 chunk를 키우는 게 빠르지 않나요?

일반적으로는 chunk가 클수록 효율적일 수 있지만, transaction 크기, lock, 메모리, 외부 처리 비용에 따라 달라집니다. 당시에는 작은 chunk가 병렬 처리와 DB 부하 분산에 더 안정적이었습니다.

### connection pool을 10에서 30으로 늘렸을 때 DB 부하는 어떻게 확인했나요?

처리 시간만 보지 않고 DB connection 사용량, query latency, timeout, CPU 부하를 함께 봐야 합니다. pool을 늘리는 것은 병렬성을 높이는 만큼 DB가 감당 가능한지 확인하는 작업이 필요합니다.

### VirtualThread를 적용한 이유는 무엇인가요?

배치 작업은 I/O 대기 시간이 많았기 때문에 platform thread를 많이 늘리는 것보다 VirtualThread가 더 적합했습니다. blocking I/O를 단순한 코드 구조로 유지하면서 동시성을 높일 수 있었습니다.

### VirtualThread가 항상 성능 개선을 보장하나요?

아닙니다. CPU-bound 작업에는 큰 이점이 없고, DB나 외부 시스템이 병목이면 오히려 부하만 키울 수 있습니다. I/O-bound이고 downstream이 감당 가능할 때 효과가 있습니다.

### bulk update를 적용하면서 transaction 크기나 lock 문제는 없었나요?

bulk update는 왕복 비용을 줄이지만 transaction 크기와 lock 범위가 커질 수 있습니다. 그래서 chunk size와 update 범위를 조절하면서 처리량과 lock 영향을 균형 있게 봐야 했습니다.

### 배치 실패 시 재시작 기준은 어떻게 잡았나요?

마지막으로 처리한 기준 id나 상태를 기준으로 재시작할 수 있어야 합니다. 전체를 처음부터 다시 돌리기보다 완료 구간과 미완료 구간을 분리하는 방식이 안전합니다.

### 배치 처리 중 일부 회원만 실패하면 어떻게 복구하나요?

실패 row를 별도로 기록하고, 전체 배치를 중단할지 skip 후 재처리할지 기준을 둬야 합니다. 회원 등급 갱신처럼 정합성이 중요한 작업은 실패 목록을 별도로 재처리하는 흐름이 필요합니다.

### 95.4% 개선 수치는 어떤 기준으로 측정했나요?

같은 배치 목적 기준으로 기존 처리 시간 19m13s와 개선 후 53s를 비교했습니다. 단순 평균보다 운영에서 실제로 의미 있는 end-to-end 처리 시간을 기준으로 봤습니다.

### 이 배치를 운영 시간에 돌려도 안전하다고 판단한 기준은 무엇인가요?

배치 시간 단축뿐 아니라 DB 부하, connection 사용량, query latency, 실패율이 허용 범위 안에 있어야 합니다. 운영 시간 실행은 성능보다 서비스 영향이 없는지가 더 중요합니다.

## 배포 / Kubernetes / 운영 자동화

### 기존 배포에서 명령 성공과 서비스 기동 성공이 달랐던 사례를 설명해 주세요.

`kubectl apply`나 Actions step은 성공했지만, 실제 애플리케이션은 초기 로컬 캐시 활성화나 설정 로딩 문제로 정상 응답하지 못할 수 있었습니다. 그래서 배포 명령 성공과 서비스 readiness를 분리해서 봐야 했습니다.

### readinessProbe는 어떤 조건을 확인하도록 구성했나요?

컨테이너가 떠 있는지만 보는 것이 아니라 애플리케이션이 요청을 받을 준비가 되었는지를 확인하도록 구성했습니다. 초기화, 설정 로딩, 내부 캐시 준비가 끝난 뒤 ready가 되도록 보는 것이 핵심입니다.

### readinessProbe와 livenessProbe의 차이를 설명해 주세요.

readinessProbe는 트래픽을 받을 준비가 되었는지 판단하고, 실패하면 service endpoint에서 제외됩니다. livenessProbe는 프로세스가 살아 있는지 판단하고, 실패하면 컨테이너를 재시작합니다.

### 초기 로컬 캐시 활성화 오류는 왜 배포 직후에 잡기 어려웠나요?

프로세스가 떠도 캐시 초기화가 끝나기 전까지는 실제 요청 처리에 실패할 수 있습니다. 단순 배포 성공만 보면 이 구간의 오류를 놓칠 수 있어서 readiness 확인이 필요했습니다.

### GitHub Actions에서 staging 자동 배포와 production 수동 승인을 나눈 이유는 무엇인가요?

staging은 빠른 검증을 위해 자동화하고, production은 사용자 영향이 크기 때문에 수동 승인 단계를 둔 것입니다. 속도와 안정성을 환경별로 다르게 가져가려는 선택이었습니다.

### 브랜드별 변경 감지 배포는 어떤 기준으로 판단했나요?

공통 모듈 변경인지, 특정 브랜드 기능 변경인지에 따라 배포 대상을 나누었습니다. 불필요한 전체 배포를 줄이고 변경 영향 범위를 명확히 하기 위한 기준이었습니다.

### 배포 중 일부 pod만 정상 기동하지 않으면 어떻게 감지하고 롤백하나요?

readiness 상태, rollout status, pod event/log를 함께 봐야 합니다. 일정 시간 안에 ready 상태가 되지 않으면 배포 실패로 보고 이전 버전으로 rollback하는 기준이 필요합니다.

### 무중단 배포에서 가장 중요하게 본 지표는 무엇인가요?

ready pod 수, error rate, latency, external dependency timeout을 함께 봅니다. 단순히 pod가 떠 있는 것보다 사용자 요청이 실패하지 않는지가 더 중요합니다.

## Spring Boot / Kotlin 기본기

### Kotlin을 Spring Boot에서 사용할 때 Java 대비 주의할 점은 무엇인가요?

Kotlin class는 기본적으로 final이기 때문에 Spring proxy를 사용하는 AOP, transaction, lazy loading과 충돌할 수 있습니다. `all-open` plugin, nullability, data class entity 사용 여부를 신경 써야 합니다.

### Kotlin의 null-safety가 실제 장애 예방에 도움이 된 사례가 있나요?

외부 API나 DB 조회 결과처럼 nullable한 값을 타입으로 표현하면 NPE 가능성을 컴파일 단계에서 줄일 수 있습니다. 다만 Java 라이브러리와 섞일 때 platform type은 별도 방어가 필요합니다.

### Spring transaction은 어떤 프록시 기반으로 동작하나요?

Spring transaction은 proxy 기반 AOP로 동작합니다. interface 기반이면 JDK Dynamic Proxy, class 기반이면 CGLIB proxy를 사용합니다.

### `@Transactional`이 적용되지 않는 대표적인 케이스를 설명해 주세요.

같은 클래스 내부에서 자기 메서드를 직접 호출하는 self-invocation에서는 proxy를 거치지 않아 적용되지 않습니다. private method나 final method도 proxy 적용에 문제가 생길 수 있습니다.

### JPA를 썼다면 N+1 문제를 어떻게 찾고 해결했나요?

SQL log, query count, APM을 통해 같은 패턴의 select가 반복되는지 확인합니다. fetch join, entity graph, batch size, DTO projection 중 조회 목적에 맞는 방식으로 해결합니다.

### connection pool exhaustion은 어떤 증상으로 나타나나요?

요청 latency가 늘고, connection acquire timeout이 발생하며, thread가 DB connection 대기 상태로 쌓입니다. DB 자체보다 애플리케이션 pool이 먼저 병목이 되는 경우도 많습니다.

### timeout은 client, server, DB, 외부 API 각각 어디에 걸어야 하나요?

각 경계마다 timeout이 필요합니다. client timeout, server request timeout, DB query/connection timeout, 외부 API connect/read timeout을 나누고, 상위 timeout이 하위 timeout보다 적절히 길어야 합니다.

### retry를 넣을 때 가장 조심해야 하는 점은 무엇인가요?

retry는 장애를 증폭시킬 수 있습니다. idempotency, retry 횟수, backoff, timeout, DLQ, 중복 처리 가능성을 함께 설계해야 안전합니다.

## 시스템 설계 질문

### 당근 동네생활 댓글/게시글 알림 시스템을 설계해 보세요.

게시글/댓글 작성은 DB transaction으로 저장하고, 알림 이벤트는 outbox나 MQ로 분리합니다. 알림 consumer가 대상자를 계산해 push를 전송하고, 실패 메시지는 retry/DLQ로 분리하는 구조가 적합합니다.

### 특정 동네에서 트래픽이 급증하면 어떻게 처리하겠습니까?

동네 id를 기준으로 cache, queue partition, rate limit 기준을 잡을 수 있습니다. hot key가 생기면 읽기 cache와 write fanout 방식을 분리하고, 알림은 비동기 큐로 흡수합니다.

### 알림 전송을 at-least-once로 설계하면 중복 문제를 어떻게 해결하겠습니까?

메시지 id와 수신자 id 기준으로 idempotency key를 만들고, 전송 이력이나 상태를 확인해 중복 발송을 줄입니다. 완전한 exactly-once보다 중복을 감지하고 방어하는 쪽이 현실적입니다.

### 게시글/댓글/알림 데이터를 MySQL과 Redis로 나눠 설계한다면 기준은 무엇인가요?

정합성이 중요한 원본 데이터는 MySQL에 둡니다. Redis는 캐시, rate limit, queue, 임시 상태처럼 빠른 접근과 만료가 중요한 데이터에 사용합니다.

### 읽기 모델과 쓰기 모델을 분리해야 하는 시점은 언제라고 보나요?

쓰기 모델을 그대로 조회할 때 join이 복잡해지고, 읽기 요구사항 때문에 쓰기 모델이 오염되기 시작하면 분리를 검토합니다. 조회 성능과 모델 명확성을 함께 봐야 합니다.

### eventual consistency를 사용자에게 허용할 수 있는 영역과 없는 영역을 나눠 보세요.

알림, 검색 색인, 통계, 어드민 조회 모델은 일정 수준의 지연을 허용할 수 있습니다. 결제, 주문 상태, 권한, 잔액처럼 사용자 신뢰와 직접 연결되는 데이터는 강한 정합성이 필요합니다.

### 장애가 났을 때 데이터 유실 없음과 즉시 응답 중 무엇을 우선할지 어떻게 판단하나요?

도메인 기준으로 판단합니다. 주문/결제처럼 유실 비용이 큰 영역은 정합성을 우선하고, 알림처럼 재처리가 가능한 영역은 빠른 응답과 비동기 복구를 선택할 수 있습니다.

## 당근 문화 / 협업 관점

### 스스로 문제를 찾았다고 했는데, 그 문제를 팀에 어떻게 설득했나요?

감으로 말하지 않고 Sentry timeout, 처리 시간, 누락 가능성, 배포 실패 지점 같은 관측 데이터를 먼저 제시했습니다. 그 다음 선택지와 운영 비용을 비교해 개선 필요성을 설명했습니다.

### 기술 선택을 할 때 팀의 운영 비용은 어떻게 고려했나요?

새 기술 도입 비용, 장애 대응 난이도, 모니터링 준비 여부, 팀의 익숙함을 함께 봤습니다. Redis Stream을 선택한 것도 기존 인프라를 활용해 운영 부담을 줄이려는 판단이었습니다.

### 혼자 개선한 내용과 팀과 같이 합의한 내용은 어떻게 구분되나요?

작은 자동화나 로컬 개선은 빠르게 직접 만들 수 있지만, 운영 구조나 데이터 흐름을 바꾸는 일은 팀 합의가 필요합니다. 사용자 영향과 장애 위험이 클수록 공유와 합의를 먼저 합니다.

### 장애 가능성을 발견했을 때 바로 고친 것과 나중으로 미룬 것은 어떤 기준으로 나눴나요?

사용자 영향, 데이터 유실 가능성, 재현 빈도, 수정 위험도를 기준으로 나눴습니다. 즉시 장애로 이어질 수 있거나 복구 비용이 큰 문제는 우선순위를 높였습니다.

### Codex를 활용한다고 했는데, AI 결과물을 어떻게 검증하나요?

AI가 만든 결과를 그대로 믿지 않고 제가 알고 있는 도메인 기준으로 검토합니다. 변경 범위, 빌드 결과, 화면 확인, 배포 상태, 보안/안정성 기준을 같이 확인합니다.

### "아는 만큼 정교하고 좋은 결과물이 나온다"는 말을 실제 업무 예시로 설명해 주세요.

Redis Stream MQ도 단순히 "비동기로 바꿔줘"가 아니라 ACK, PEL, 중복, DLQ, outbox까지 알아야 안정적인 구조가 나옵니다. 도메인과 장애 지점을 이해해야 AI나 도구를 써도 좋은 결과가 나온다고 생각합니다.

### 본인이 만든 자동화가 오히려 팀에 부담이 된 적은 없었나요?

자동화는 이해하기 어렵거나 실패 원인이 불명확하면 부담이 됩니다. 그래서 배포 자동화도 Slack 알림, rollout 상태, readiness 확인처럼 실패 지점을 볼 수 있게 만드는 것이 중요했습니다.

## 꼬리질문

### "알림 누락 0% 수준"을 어떻게 측정했나요?

알림 produce, consume, ACK, 외부 API 전송 결과를 기준으로 누락 가능 구간을 추적했습니다. 운영 관측 범위 안에서 재시작과 timeout 이후에도 미처리 메시지를 회수할 수 있게 만든 것을 기준으로 말한 수치입니다.

### "0%"라고 말할 수 있는 관측 범위는 어디까지인가요?

모든 상황에서 절대적 0%를 보장한다는 의미는 아닙니다. 관측 가능한 주문 알림 처리 흐름과 운영 기간 내에서 누락 사례가 재현되지 않도록 안정화했다는 의미로 설명하는 것이 정확합니다.

### Redis Stream 장애 시 fallback은 있었나요?

Redis 장애 시 즉시 전송은 어려울 수 있으므로 outbox와 재적재 흐름이 중요합니다. Redis가 복구된 뒤 outbox 기준으로 누락 produce를 다시 수행할 수 있게 하는 방향이 fallback입니다.

### CDC 파이프라인에서 정합성 검증은 자동화되어 있었나요?

완전한 자동 검증보다 주요 key와 샘플 기준 검증이 현실적이었습니다. 더 개선한다면 원본 MySQL과 MongoDB 조회 모델의 count/checksum 비교를 주기적으로 자동화할 것 같습니다.

### MongoDB 역정규화 데이터가 원본 MySQL과 달라졌을 때 복구 방법은 무엇인가요?

원본은 MySQL이므로 특정 key 범위를 기준으로 재동기화하거나 증분 스냅샷을 다시 실행해 MongoDB 조회 모델을 복구합니다. 조회 모델은 재생성 가능하게 설계하는 것이 중요합니다.

### VirtualThread 도입 후 thread dump나 모니터링 방식은 달라졌나요?

VirtualThread는 수가 많기 때문에 기존 thread 수만 보는 방식은 의미가 줄어듭니다. 처리량, DB connection 사용량, latency, 실패율처럼 실제 병목 지표를 중심으로 봐야 합니다.

### 배치 성능 개선이 DB 전체 성능에 악영향을 주지 않았다는 근거는 무엇인가요?

처리 시간이 줄어도 DB 부하가 과도하면 실패입니다. query latency, connection 사용량, timeout, 운영 요청 영향이 허용 범위 안에 있는지 함께 봐야 개선이라고 말할 수 있습니다.
